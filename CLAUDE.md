# CLAUDE.md

## 프로젝트 목적

**가나 스윔의 시즌 중간 상품 운영 성과와 재고 건전성**을 분석하고 모니터링한다.

- 분석 대상: 시즌이 끝난 뒤의 결산이 아니라, **시즌이 진행 중인 시점에서의 성과와 재고 상태**
- 두 축을 함께 본다
  - **상품 운영 성과** — 무엇이 팔리고 있는가, 실제로 이익이 남는가
  - **재고 건전성** — 재고가 과한가 부족한가, 시즌 안에 소진 가능한가

### 세부 목적
가나 스윔 시즌 중간 상품 운영 성과와 재고 건전성을 **진단**하고, **재고 액션 검토 목록**을 만든다.

- 최종 산출물은 두 가지다
  1. **진단 결과** — 성과·재고 건전성에 대한 분석 결론
  2. **재고 액션 검토 목록** — 진단에 근거해 검토가 필요한 대상과 권고 액션
- **대시보드는 분석 목적이 아니라 모니터링 구현물이다.** 분석은 분석 단계에서 끝내고, 대시보드는 그 결과를 지속 관찰하기 위한 도구로만 만든다

---

## 확정 규칙 (분석 전제)

원본 점검 결과에 대해 확정된 판단이다. 분석·코드·대시보드 모두 이 규칙을 따른다.
근거는 `outputs/reports/xlsx_structure_review.md` 참조.

### 1. 분석 기준일 = **2026-07-31**
- 모든 "현재"·"최근"의 기준은 이 날짜다
- **실행 시점의 오늘 날짜를 쓰지 않는다.** 상수로 고정해 재현성을 확보한다

### 2. 기준일 이후 날짜는 **미발생으로 간주**
- 기준일(2026-07-31)을 넘는 이벤트 9건은 **아직 일어나지 않은 것으로 처리**한다
  - `order_item.cancel_date` 2건 (~2026-08-02) → 취소되지 않은 것으로 취급
  - `returns.처리일` 7건 (~2026-08-06) → 반품 미완료(접수 상태)로 취급
- 즉 **완료 반품 환불액에 포함하지 않는다**

### 3. 매출·이익 집계 단위 = **주문(`orders`) 단위**
- 매출·공헌이익의 기준 grain은 **주문(`order_id`)**이다
- 금액 컬럼은 `order_item`에만 존재하므로, **`order_item` 금액을 `order_id` 기준으로 합산한 뒤** 주문 단위로 집계한다
- 주문 수는 `orders`, 금액은 위 합산값을 사용해 이중 집계를 방지한다

### 4. 발주 상태 = **현재 발주 중단**
- 2026년 6~7월 발주 0건은 데이터 누락이 아니라 **실제 발주 중단 상태**다
- 리오더 판단 시 "발주 데이터 없음"을 결측이 아닌 **중단이라는 사실**로 해석한다

---

## 작업 진행 방식 (반드시 준수)

### 규칙 1. 한 단계씩만 진행하고 멈춘다
각 단계의 분석을 마무리한 뒤 **다음 분석을 자동으로 이어서 수행하지 않는다.**

- 한 번의 응답에서 하나의 분석 단계만 수행한다
- 단계가 끝나면 결과를 보고하고 **멈춘다**
- 사용자가 명시적으로 다음 단계를 지시할 때까지 기다린다

### 규칙 2. 다음 분석은 "추천"만 한다
단계 결과 보고 뒤에는 다음에 분석할 것을 **추천**한다. 추천은 제안이지 실행 예고가 아니다.

- 추천 항목은 2~4개, 각각 "무엇을 / 왜 / 무엇을 알 수 있는지" 형태로 제시
- 추천한 분석을 먼저 진행하지 않는다
- "이어서 진행하겠습니다" 같은 표현을 쓰지 않는다

### 규칙 3. 작업 전에 계획을 먼저 알린다
파일 생성·수정, 명령 실행 전에 무엇을 할지 계획을 먼저 제시하고 확인을 받는다.

---

## 데이터 처리 규칙

### 원본은 절대 수정하지 않는다
`data/raw/` 아래 원본 데이터는 **읽기 전용**이다. 어떤 경우에도 덮어쓰지 않는다.

### 분석 결과는 중간 산출물로 분리 저장한다
모든 분석 결과는 원본과 별개의 파일로 저장한다.

| 저장 위치 | 용도 |
|---|---|
| `data/converted/` | 원본 XLSX 시트를 **가공 없이** CSV로 변환한 사본 (`src/convert_xlsx.py`가 생성) |
| `data/interim/` | 정제·가공 진행 중인 중간 데이터 |
| `data/processed/` | 정제 완료, 대시보드가 읽는 최종 데이터 |
| `outputs/tables/` | 분석 단계별 집계 결과 (지표 테이블) |

**파일 이름 규칙**: `{단계번호}_{분석내용}.csv` (예: `01_cleaned_base.csv`, `02_sales_by_channel.csv`)

**저장 형식**: CSV, `encoding="utf-8-sig"` (Excel에서 한글 깨짐 방지), `index=False`

데이터 흐름은 한 방향이다:
```
data/raw (원본 XLSX, 불변)
   → data/converted (시트별 CSV, 값 무가공)
      → data/interim (가공 중)
         → data/processed (최종)
            → outputs/tables (분석 결과 집계)
```

### `data/converted/`의 규칙
원본과 정제 사이의 **무가공 변환 단계**다. 원본 XLSX를 직접 읽는 대신 이 단계를 거친다.

- **값을 바꾸지 않는다.** 시트명·열 순서·값을 원본 그대로 유지하고, ID는 문자열로 보존한다
- 결측 보정·타입 캐스팅·열 이름 표준화는 **`interim` 이후에서** 한다
- 생성·검증은 `src/convert_xlsx.py`로만 한다. 손으로 만들거나 고치지 않는다
- 재생성 명령: `.venv/bin/python -m src.convert_xlsx`
- 검증 결과는 `outputs/reports/conversion_validation.csv`에 남는다. **한 시트라도 `실패`면 다음 단계로 넘어가지 않는다**

---

## 설명 방식 (비개발자·비전공자 기준)

분석 결과를 설명할 때는 **실무 담당자나 경영진이 읽고 바로 이해할 수 있어야** 한다.

### 반드시 포함할 것
1. **결론 먼저** — 숫자를 나열하기 전에 "그래서 무슨 뜻인지"를 한 문장으로 먼저 쓴다
2. **용어 풀이** — 분석 용어가 나오면 그 자리에서 괄호나 표로 풀어준다
   - 예: "중위값 (줄을 세웠을 때 정확히 가운데 있는 값. 평균과 달리 극단적으로 높거나 낮은 값에 흔들리지 않음)"
3. **숫자의 의미** — 비율만 쓰지 말고 실제 규모를 함께 쓴다 (예: "28% (10건 중 약 3건)")
4. **표본 크기 경고** — 건수가 적은 그룹의 비율은 신뢰하기 어렵다는 점을 반드시 명시
5. **판단 근거와 한계** — 이 숫자로 말할 수 있는 것과 말할 수 없는 것을 구분

### 설명은 프롬프트 출력으로만 한다
자세한 설명과 용어 풀이는 **채팅 응답에만 출력하고, 파일로 저장하지 않는다.**

- 파일(CSV)에는 데이터만 담는다
- 코드 주석은 개발자용 최소 수준으로만 유지한다
- 설명 문서(md 등)를 요청받지 않은 상태에서 만들지 않는다

### 상관관계 ≠ 인과관계
"A의 수치가 높다"는 사실이고, "A 때문이다"는 추측이다. 사실과 해석을 반드시 구분해서 표현한다.

---

## 폴더 구조

```
MD_dashboard/
├── data/
│   ├── raw/          # 원본 XLSX (읽기 전용)
│   ├── converted/    # 시트별 CSV 사본 (값 무가공, 자동 생성)
│   ├── interim/      # 가공 중 중간 산출물
│   └── processed/    # 최종 분석 데이터
├── notebooks/        # 탐색적 분석
├── src/
│   ├── config.py     # 경로·상수 단일 관리 (모든 경로는 여기서만 정의)
│   ├── convert_xlsx.py  # raw XLSX → converted CSV 변환·검증
│   ├── data_loader.py
│   ├── preprocess.py
│   ├── metrics.py    # 지표 계산 함수
│   └── viz.py
├── dashboard/
│   ├── app.py        # Streamlit 메인
│   ├── pages/
│   └── components/
├── outputs/
│   ├── figures/      # 차트 이미지
│   ├── tables/       # 분석 결과 CSV
│   └── reports/
├── logs/
└── tests/
```

### 코드 작성 원칙
- **경로는 `src/config.py`에서만 정의한다.** 상대경로 하드코딩 금지 (실행 위치가 달라도 동작해야 함)
- **지표 계산은 `src/metrics.py`에 모은다.** 대시보드에서 계산 로직을 중복 작성하지 않는다
- **대시보드는 계산하지 않는다.** `processed` 데이터를 읽고 `metrics.py`를 호출만 한다
- **대시보드는 실패해도 안내한다.** 데이터 파일이 없으면 크래시하지 않고 "먼저 `python -m src.preprocess`를 실행하세요"를 표시한다

---

## 실행 환경

```bash
# 가상환경
source .venv/bin/activate

# 분석 실행
.venv/bin/python -m src.preprocess

# 대시보드 실행
.venv/bin/streamlit run dashboard/app.py

# 테스트
.venv/bin/python -m pytest tests/ -v
```

---

## Git 정책

- 원격: `https://github.com/redskio/md_dashboard` (**PUBLIC 저장소**)
- `data/` 전체를 gitignore한다
- `outputs/`, `logs/`, `.venv/`는 커밋하지 않는다
- push 전에 스테이징 목록을 확인해 민감 데이터가 포함되지 않았는지 검증한다
